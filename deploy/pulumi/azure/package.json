{
  "name": "@artifact-server/azure-deployment",
  "version": "0.0.0",
  "private": true,
  "description": "Pinned Pulumi deployment project for Artifact Server on Azure.",
  "type": "module",
  "packageManager": "pnpm@10.34.3",
  "engines": {
    "node": ">=24.12.0"
  },
  "scripts": {
    "preview": "pulumi preview",
    "test": "vitest run tests/azure-pulumi.test.ts",
    "test:evidence": "vitest run tests/azure-pulumi.test.ts --reporter=default --reporter=json --outputFile.json=../../../evidence/azure-pulumi.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "verify": "pnpm typecheck && pnpm test:evidence"
  },
  "dependencies": {
    "@pulumi/azure-native": "3.26.0",
    "@pulumi/pulumi": "3.257.0",
    "@pulumi/random": "4.21.1",
    "effect": "4.0.0-rc.108"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "ts-node": "10.9.2",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  }
}
